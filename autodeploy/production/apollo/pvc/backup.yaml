apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: production
  name: backup-pvc
spec:
  storageClassName: custom
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
