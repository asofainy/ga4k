apiVersion: apps/v1
kind: Deployment
metadata:
  name: calibre-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: calibre-web
  template:
    metadata:
      labels:
        app: calibre-web
    spec:
      containers:
      - name: calibre-web
        image: linuxserver/calibre-web:0.6.19-ls177
        imagePullPolicy: Always
        ports:
        - containerPort: 8083
        volumeMounts:
        - name: calibre-web-data
          mountPath: /config
        - name: books
          mountPath: /books
      volumes:
        - name: calibre-web-data
          persistentVolumeClaim:
            claimName: calibre-web-data-pvc
        - name: books
          persistentVolumeClaim:
            claimName: books-pvc
