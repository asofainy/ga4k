apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc-backup
spec:
  storageClassName: custom
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
