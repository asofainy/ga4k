apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: test
  name: redmine-files-pvc
spec:
  storageClassName: custom
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
